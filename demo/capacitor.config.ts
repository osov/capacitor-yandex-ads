import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.osov.yandexads.demo',
  appName: 'YandexAdsDemo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
